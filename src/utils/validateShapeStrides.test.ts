import { validateShapeLayout } from "./validateShapeStrides"

describe("ValidateShapeLayout", () => {
  test.each([
    [
      // shape
      [1, 1, 4, 2],
      [],
      [],
    ],
  ])("test %s", (shape, dataElements, expected) => {})
})
